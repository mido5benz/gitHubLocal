/* eslint-disable @typescript-eslint/no-unused-vars */

import {VehicleTypePipe} from './vehicleType.pipe';

describe('Pipe: VehicleTypee', () => {
  it('create an instance', () => {
    const pipe = new VehicleTypePipe();
    expect(pipe).toBeTruthy();
  });
});
