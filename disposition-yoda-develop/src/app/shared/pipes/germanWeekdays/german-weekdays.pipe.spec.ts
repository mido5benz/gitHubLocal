import { GermanWeekdaysPipe } from './german-weekdays.pipe';

describe('GermanWeekdaysPipe', () => {
  it('create an instance', () => {
    const pipe = new GermanWeekdaysPipe();
    expect(pipe).toBeTruthy();
  });
});
