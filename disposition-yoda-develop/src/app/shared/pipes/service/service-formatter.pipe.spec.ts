import {ServiceFormatterPipe} from './service-formatter.pipe';

describe('ServiceFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
