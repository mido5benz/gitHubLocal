import {SollStoppNumberPipe} from './soll-stopp-number.pipe';

describe('SollStoppNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SollStoppNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
