import { WeekdaynumberPipe } from './weekdaynumber.pipe';

describe('WeekdaynumberPipe', () => {
  it('create an instance', () => {
    const pipe = new WeekdaynumberPipe();
    expect(pipe).toBeTruthy();
  });
});
