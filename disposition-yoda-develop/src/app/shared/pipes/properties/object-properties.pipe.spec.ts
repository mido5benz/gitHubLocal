import {ObjectPropertiesPipe} from './object-properties.pipe';

describe('ObjectPropertiesPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectPropertiesPipe();
    expect(pipe).toBeTruthy();
  });
});
